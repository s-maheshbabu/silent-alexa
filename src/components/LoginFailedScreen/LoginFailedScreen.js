import React from "react";

export default function LoginFailedScreen(props) {
  return (
    // TODO
    "Login Failed. This is just a placeholder. We might want to redirect the user to the login page or at least have a link here to let the user try again or go home."
  );
}
